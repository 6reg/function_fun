/*
let value = arr.reduce(accumulator, item, index, array) {
	// ...
}, [initial]);
*/

// args
// * accumulator: result of previous function call, equals initial the first time
// * index: position
// * array: array

//let arr = [1,2,3,4,5];
//let res = arr.reduce((sum, current) => sum + current, 0);
//console.log(res)

function reducer(sum, current, arr) {
	let total = [];
	for (key in arr) {
	total.ZZ

